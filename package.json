{
  "name": "exchange-github-token",
  "version": "1.3.0",
  "license": "Apache-2.0",
  "author": {
    "name": "abi group GmbH",
    "email": "info@abigroup.io",
    "url": "https://abigroup.io/"
  },
  "type": "module",
  "files": [
    "action.yml",
    "README.md",
    "CHANGELOG.md",
    "LICENSE.md"
  ],
  "scripts": {
    "format:check": "prettier --check '*.{md,json,json5,yaml,yml}'",
    "format:fix": "prettier --write '*.{md,json,json5,yaml,yml}'"
  },
  "lint-staged": {
    "*.{json,json5,yaml,yml,md}": [
      "prettier --write"
    ]
  },
  "prettier": "@abinnovision/prettier-config",
  "dependencies": {
    "setup-oidc-token-cli": "^1.1.3"
  },
  "devDependencies": {
    "@abinnovision/prettier-config": "^2.2.0",
    "prettier": "^3.9.5"
  }
}